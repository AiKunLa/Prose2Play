# AGENTS

本目录负责核心领域模型和不变量。

## 规则

- 领域对象优先表达业务语义，而不是框架细节
- 状态机流转规则只能在领域层定义，不要散落在 API 或 Worker
- 稳定 ID、scene 编号、source_refs 等核心不变量必须在这里被保护
- 新增核心实体或状态时，应同步补测试

## 不应该做什么

- 不要直接依赖具体 LLM SDK
- 不要在这里放 HTTP、数据库或前端实现细节
